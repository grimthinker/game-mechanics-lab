/**
 * Конфигурация параметров процедурной травы и ее генерации
 */
export const GRASS_CONFIG = {
  /** Значение коэффициента плотности по умолчанию (от 0.0 до 1.0) */
  defaultDensityFactor: 0.8,

  /**
   * Порог плотности красного канала (0..255) в splatmap террейна.
   * Если значение ниже этого порога, точка считается переходом/голой землей и трава не спавнится.
   */
  densityThreshold: 80,

  /** Максимальная вместимость пулов инстансов при плотности 100% (densityFactor = 1.0) */
  capacities: {
    blade3: 40000,
    blade4: 25000,
    blade5: 15000,
  },

  /**
   * Независимые целевые вероятности появления вариантов (в сумме 1.0 = 100%).
   * blade3 — самые частые, blade4 — средние, blade5 — самые редкие.
   */
  probabilities: {
    blade3: 0.55,
    blade4: 0.3,
    blade5: 0.15,
  },
} as const;
